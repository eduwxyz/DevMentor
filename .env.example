# DevMentor Environment Variables

# Node Environment
NODE_ENV=development

# Application Configuration
# Port for Next.js development server (default: 3000)
PORT=3000

# Gemini CLI Configuration
# The Gemini CLI must be installed globally and authenticated
# Install: npm install -g @google/generative-ai-cli
# Authenticate: gemini auth login

# Gemini Model (used in API calls)
# Default model used for chat and code review
GEMINI_MODEL=gemini-3-flash-preview

# Database Configuration
# SQLite database path for user progress and gamification
DATABASE_PATH=./data/devmentor.db

# Workspace Configuration
# Base directory for user project workspaces
WORKSPACE_DIR=./workspaces

# Projects Configuration
# Base directory for project templates
PROJECTS_DIR=./projects

# Feature Flags (optional)
# Enable/disable experimental features
ENABLE_MULTIPLAYER=false
ENABLE_AUTO_VALIDATION=false
